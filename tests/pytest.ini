[pytest]
# Pytest configuration for mkx tests

testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Coverage settings
addopts = 
    -v
    --strict-markers
    --tb=short

markers =
    unit: Unit tests for individual modules
    integration: Integration tests requiring multiple components
